//Exercício 15

//Crie um array de números e use map() para multiplicar cada elemento por 2.
let numeros = [1, 2, 3, 4, 5];
let multiplicados = numeros.map(num => num * 2);
console.log(multiplicados);
//[2,4,6,8,10]
